name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-26.04

    steps:
      - uses: actions/checkout@v7.0.1
      - uses: borales/actions-yarn@v5.0.0
        with:
          cmd: install
      - uses: borales/actions-yarn@v5.0.0
        with:
          cmd: test
