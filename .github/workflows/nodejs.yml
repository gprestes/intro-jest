name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4.1.7
      - uses: borales/actions-yarn@v4.2.0
        with:
          cmd: install
      - uses: borales/actions-yarn@v4.2.0
        with:
          cmd: test
